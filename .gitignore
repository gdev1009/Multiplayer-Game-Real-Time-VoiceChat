# Secrets / environment
.env
**/.env
*.keystore
*.jks
# Local scratchpad containing live credentials — never commit
docs/Project.md
docs/Internal_note.md
**/google-services.json
**/GoogleService-Info.plist

# App Store Connect API keys (Codemagic secrets — never commit)
**/AuthKey_*.p8
**/*.p8
docs/AuthKey_*.p8
secrets/
**/certificate_private_key.pem
**/asc_distribution_key
**/asc_distribution_key.pub

# Flutter / Dart build artifacts
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/
app/.packages
app/pubspec.lock
**/*.g.dart

# IDE
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Logs / coverage
coverage/
*.log

# Local recovery / delivery artifacts
.git_corrupt_backup_*/
app/dist/

# JS tooling (frame extraction scripts, etc.)
node_modules/
**/node_modules/

# Python bytecode / local tool backups
**/__pycache__/
app/tools/collar_fix_backup/
docs/SVID_*.mp4
